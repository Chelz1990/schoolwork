#include <iostream>

using namespace std;

int main () {
    int
        x,
        y;
    cout << "Enter your first number" << endl;
    cin >> x;
    cout << "Enter a second number" << endl;
    cin >> y;

    for ( int i = x; i <= y; i++) {
        if (i % 2 == 0)
        cout << i;
    }
}
